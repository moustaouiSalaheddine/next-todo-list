import { Box, Card, Typography } from "@mui/material";
import { error } from "console";

const UserAnalytics = () => {
  return (
    <Card sx={{ p: 1, height: 510 }}>
      <Typography variant="h6" gutterBottom>
        User Analytics
      </Typography>
    </Card>
  );
};

export default UserAnalytics;
